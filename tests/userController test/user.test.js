const showCurrentUser = require("../../controllers/userController/userController");
const getAllUsers = require("../../controllers/userController/userController");
const getUser = require("../../controllers/userController/userController");
const updateUser = require("../../controllers/userController/userController");
const deleteUser = require("../../controllers/userController/userController");

test("should register a user to be authorized", () => {});
test("should register a user to be authorized", () => {});
test("should register a user to be authorized", () => {});
test("should register a user to be authorized", () => {});
