const getAllProductsStatic = require("../../controllers/productController/productStoreController");
const getAllProducts = require("../../controllers/productController/productStoreController");

test("should register a user to be authorized", () => {});
test("should register a user to be authorized", () => {});
